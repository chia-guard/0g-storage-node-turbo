name: 0g-storage

services:
  node:
    image: ghcr.io/lushdog/0g:latest
    network_mode: 'host'
    volumes:
      - ./log:/app/0g-storage-node/run/log
    environment:
      - RPC=${RPC}
      - PRIVATE_KEY=${PRIVATE_KEY}